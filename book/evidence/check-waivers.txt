# Standing exceptions for book/evidence/check.sh.
#
# One line per waived violation, written as:
#
#   <rule> <subject> — owner: <task or person> — <why it is still true>
#
# The checker prints every waiver it applies, so a waiver is a visible debt, not
# a silent bypass. Delete a line when its owner fixes the cause; do not widen a
# rule to avoid adding one.

figure-number fig-placement-ownership.svg — owner: task book/figures-v2 — the PlantUML source draws "Figure 1 — placement and ownership" while Typst also numbers the figure, so the page shows two numbers. Remove the label from book/assets/sources/fig_placement_ownership.puml.

figure-number fig-cache-stale-fill.svg — owner: task book/figures-v2 — the PlantUML source draws "Figure 3 — the stale-fill race and the fence" while Typst also numbers the figure. Remove the label from book/assets/sources/fig_cache_stale_fill.puml.
