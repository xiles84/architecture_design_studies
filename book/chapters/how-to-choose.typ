#import "../lib/config.typ": *
#import "../lib/evidence.typ": registry-card

#heading("How to choose")

#marker("chapter", "how to choose")
This chapter holds the decision path a reader follows before any family detail. Its prose is written
by the HIGH `book-synthesis-v1` task; the structure and the evidence hooks below are fixed here.

#heading(level: 2, "Answerability before performance")
#direct[
  The first question is what the schema must be able to *answer*, not how fast it answers. Two
  measured answerability failures make this a rule rather than a preference.
]
#registry-card("v2-07-refund-answerability-and-storage")
#registry-card("v2-gap-02-hold-funnel-unanswerable")

#heading(level: 2, "The decision path")
#list(
  [Name the questions the data must answer, including the ones no current report answers.],
  [Place each fact once, in the family that answers its questions most directly.],
  [Choose an arbitration mechanism for the contended surface.],
  [Accept a maintenance cost for any derived copy, or do not create it.],
  [State the evidence level and the coverage gap beside the recommendation.],
)
