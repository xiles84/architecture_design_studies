#import "../lib/config.typ": *
#import "../lib/evidence.typ": registry-card

#heading("Cross-family comparisons")

#marker("chapter", "cross-family comparisons")
Comparisons here are controlled pairs or explicitly labelled analogy. No cross-study numeric ranking
appears: no comparability record exists. The six confounds that bound every comparison are published
beside the numbers, not in a footnote.
#gap[
  Several family contrasts are family/package comparisons rather than one-decision pairs; see the
  confound register `book/evidence/v2/confounds.json`.
]
#registry-card("v2-03-embedding-not-ahead")
#registry-card("v2-11-section-seat-map")
