#import "../lib/config.typ": *
#import "../lib/evidence.typ": registry-card

#heading("Scenarios")

#marker("chapter", "scenarios")
Scenarios group the studies by the reader's domain: a charity donation tree, ticket overbooking
and refunds, reserved seating and holds, a configuration portal, and an external cache. Each carries
its own answerability questions and its own coverage gaps.
#registry-card("v2-06-hot-drop-designs")
#registry-card("v2-10-guarded-confirm-refusals")
#registry-card("v2-15-three-instance-staleness")
