#import "../lib/config.typ": *
#import "../lib/evidence.typ": registry-card

#heading("Derived state and history")

#marker("concept", "derived state and history")
Rollups, duplicated keys and append-only histories are all derived state with different maintenance
costs and different answerability. History is the only one that can answer a question after the
event was undone.
#registry-card("v2-13-trigger-rollup-cost")
#registry-card("v2-09-postgres-ledger-ratio-unsettled")
