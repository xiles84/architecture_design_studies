#import "../lib/config.typ": *
#import "../lib/evidence.typ": registry-card

#heading("Reading benchmarks honestly")

#marker("concept", "reading benchmarks")
Closed vs open loop, single vs repeated trials, per-node vs equal-total framing, throttled cores,
and the difference between a design effect and a cell-order effect. This concept is the reader's
guard against quoting a number the run cannot support.
#registry-card("v2-16-strict-freshness-read-cost")
#registry-card("v2-gap-01-ledger-load-multiplier-unstable")
#registry-card("v2-gap-04-study05-unmeasured-regimes")
