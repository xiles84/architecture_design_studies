#import "../lib/config.typ": *
#import "../lib/evidence.typ": registry-card

#heading("Information placement")

#marker("concept", "information placement")
Where a fact lives decides what it can answer and what it costs to change. This concept frames the
derive → index → materialize → copy/cache ladder and the answerability-first rule.
#registry-card("v2-07-refund-answerability-and-storage")
#registry-card("v2-02-rollup-family")
