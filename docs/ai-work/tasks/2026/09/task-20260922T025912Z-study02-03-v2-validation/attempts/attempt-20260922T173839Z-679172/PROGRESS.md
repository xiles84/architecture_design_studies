# Progress — Study 02/03 v2 HIGH validation

**Task:** `task-20260922T025912Z-study02-03-v2-validation`
**Branch:** `repo/study02-03-v2-high-validation`
**Attempt:** `attempt-20260922T173839Z-679172` (claim `claim-af4ef40a3d7a6251`, epoch 1)
**Capability/role:** HIGH / reviewer (model `deepseek-flash`, tool/effort not exposed)
**Required tag:** `repo/study02-03-v2-high-validation`

This is an artifact-only review. No database was started and no measurement was run.

## Decision Log

1. **Validate against the run JSON, not the generated report.** Every figure was re-extracted
   from `studies/0{2,3}-*/results/<run>/<topology>/<design>.json` before being compared to the
   report and the signed analyses. This is what caught the two claim defects below; reading the
   report alone would have repeated them.
2. **One cross-run analysis per study, indexed under every run it covers.** The Study 02
   validation is one file with `run_id: 20260921T205212Z` and an `inputs:` line naming
   20260921T183722Z and 20260920T234953Z, so all three reports index it. I first proved each
   report regenerates byte-identically from the committed image (`localhost/ticketbench:1` =
   `a24c10933bd0`, `localhost/seatbench:1` = `a145af66f330`) so only the analyses row changed.
   *Alternative considered:* three per-run analyses — rejected as restating one review three times.
3. **`claims.json` (v1) was not edited.** The registry is a protected v1 artifact and the
   correction task owns its replacement. Verdicts go to a new file,
   `book/evidence/reviews/20260922-phase-3b-high-validation.json`. Confidence: High.
4. **Two derived-claim defects are recorded, not repaired in place.** `claim-07`'s "1.7-2.1x" is
   the sibling arm's range (the run's own tables give 1.7-5.7x), and `claim-05`'s "8x load time"
   is contradicted by the two sibling runs (1.2x, same loader code). Both are corrections of
   *derivation*, so they belong to the evidence-correction task; this review states them and the
   exact evidence. Confidence: High (re-derived from JSON).
5. **The 32-buyer PostgreSQL sign flip is left unresolved on purpose.** Buyer count and design
   order changed together, so no attribution is possible from the collected data; the review
   says so rather than picking a winner. Confidence: High that the confound exists.
6. **No new measurement is proposed as required.** The load-multiplier question is named as the
   cheapest follow-up, but nothing in this review depends on measuring it; the task may not start
   a database.
7. **Validation verdict for `claim-gap-04` is `retired`.** Its entire content is "validation is
   still open", which this task falsifies. The v2 registry should drop or supersede it.

## What was produced

- `studies/02-ticket-booking/reports/analyses/20260921T205212Z--deepseek-flash--2026-09-22.md`
- `studies/03-reserved-seating/reports/analyses/20260921T231328Z--deepseek-flash--2026-09-22.md`
- `book/evidence/reviews/20260922-phase-3b-high-validation.json`
- Regenerated reports so each index lists the new analysis (one added row per report).
- `CONTEXT.md` and `LESSONS_LEARNED.md` updated.

## Acceptance evidence

- Gates: 46/46 (P3), 48/48 (X1) in every Study 02 cell; Study 03 65/65 in all 41 cells; 0 failed
  cells in 3b-3/3b-4, 1 recorded partial cell in 3b-2.
- Ledger audits after every writing phase: 26 per X1 cell (26/26/18 in 3b-2), all 0 mismatches
  and 0 attribution mismatches.
- Post-write report checks: 3 (P3) / 6 (X1) per cell, 0 failures in both arms.
- Control power demonstrated in dev checks (`am03-dc01-ledger-controls`, `am04-dc01-{pg,yb}`).
- `go run . validate --repo <worktree>` in the pinned Go image → `16 claims, 0 errors`.

## Residual risks / limitations

- No control fired inside any measured run; the audit's sensitivity at `small` contention is
  inherited from `tiny` dev checks.
- Single run per arm, one trial per cell at the design level, quota-bound engines, closed loop,
  no real network. The validated ratios are within-run and directional.
- Report regeneration used the recorded images; the three Study 02 reports and the Study 03
  report regenerated byte-identically apart from the analyses index row.
