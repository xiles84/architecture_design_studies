# Result — Study 02/03 v2 HIGH validation

**Task:** `task-20260922T025912Z-study02-03-v2-validation`
**Branch:** `repo/study02-03-v2-high-validation`, base `c56eca7`
**Required tag:** `repo/study02-03-v2-high-validation`
**Capability:** HIGH / reviewer (model `deepseek-flash`)
**Benchmark:** none — artifact-only review, no database started

## What was delivered

1. A signed **Study 02 phase-3b HIGH validation** analysis
   (`studies/02-ticket-booking/reports/analyses/20260921T205212Z--deepseek-flash--2026-09-22.md`),
   indexed under all three phase-3b Study 02 runs through its `inputs:` frontmatter.
2. A signed **Study 03 phase-3b HIGH validation** analysis
   (`studies/03-reserved-seating/reports/analyses/20260921T231328Z--deepseek-flash--2026-09-22.md`),
   indexed under the reports run.
3. A machine-readable **claim verdict file**
   (`book/evidence/reviews/20260922-phase-3b-high-validation.json`) with per-claim
   accepted / narrowed / out-of-scope / retired verdicts and the required corrections.
4. Regenerated reports (Study 02 3b-1/3b-2/3b-3 and Study 03 3b-4) so each index lists the new
   analysis; each regenerated byte-identically from the committed image plus one analyses row.
5. `CONTEXT.md` and `LESSONS_LEARNED.md` updates recording that the validation is closed.

## Verdicts (summary)

| Claim | Verdict |
|---|---|
| `claim-04-hot-drop-sellout` | out of scope (Study 02 v1 matrix) |
| `claim-05-refunds-unanswerable-without-ledger` | **narrowed** — keep answerability + `+33-35%` storage; drop the "8x load" clause |
| `claim-06-ledger-cost-128-buyers` | **accepted** (128-buyer arm; every figure re-derived) |
| `claim-07-p3-trials-disagree` | **narrowed** — the range is 1.7-5.7x, not the headline's 1.7-2.1x |
| `claim-08-guarded-confirm-refusals` | out of scope (Study 03 v1 matrix) |
| `claim-09-section-seat-map` | **accepted** (all three clauses re-derived) |
| `claim-gap-03-hold-window-sizing` | **accepted** |
| `claim-gap-04-study02-03-validation-open` | **retired** — this review is the validation |

## Acceptance checks re-run

- **Gates.** Study 02: every cell passes 46/46 (P3) and 48/48 (X1); 3b-3 has 0 failed cells.
  3b-2's `yb-cluster3/x1_cas_ledger` is a partial cell (race-trial RPC timeout) and is listed in
  its manifest `failed_cells`; it is not a gate failure and not a stop condition.
  Study 03: 41 cells, 0 failed, 65/65 in every cell.
- **Ledger audits (AM-03.2).** 26 ledger audits per X1 cell in 3b-3 (26/26/18 in 3b-2), every
  one 0 net mismatches and 0 attribution mismatches, after every write/race/churn tier.
- **Post-write report checks (AM-03.5).** 3 (P3) / 6 (X1) checks per cell, 0 failures in both
  arms on all three topologies.
- **Control power (AM-03.3/AM-04.4).** Injected `drop-sale` and `wrong-customer` faults fail the
  gate (`am04-dc01-pg/fault-*.log`); the pre-AM-04 natural control fires on the unfixed SQL
  (`am03-dc01-ledger-controls/step1-natural-control-unfixed.log`). No control ran inside the
  measured runs — recorded as a residual gap.
- **Evidence registry.** `go run . validate --repo <worktree>` in the pinned Go image →
  `evidence: 16 claims, 0 errors, 16 book inputs`.

## Material defects found and how they are handled

- **`claim-07` range.** The 32-buyer arm's headline says "1.7-2.1x"; its own tables give 2.05-5.73x
  on `yb-single` and 1.67-3.00x on `yb-cluster3`. Its conclusion-3 cancel figures are also the
  sibling's. The analysis is signed and not edited; the correction is recorded for the
  evidence-correction task.
- **`claim-05` "8x load".** The same loader and pair measure 1.2x in the two sibling runs.
  Recorded as a required correction; storage and answerability survive.
- Neither defect weakens the phase-3b runs themselves: the gates, audits and post-write checks
  all passed, and the measurements are unchanged.

## Outcome

The Study 02 and Study 03 phase-3b claims are **complete and HIGH-validated** with the
qualifications above. No measurement is authorized by this review and none is needed to close it.

**Files:** see `attempts/attempt-20260922T173839Z-679172/PROGRESS.md` for the Decision Log.
