# Execution brief — publish the book/queue handoff

Archive the owner's plan and HIGH's decisions in an isolated worktree. Define the queue
workflow and schemas sufficiently for a LOW session to implement them without reopening
architecture decisions. Publish ordered task records and proposed future-study records,
update `AGENTS.md` with session capability selection, validate the archive, commit, create
the annotated planning tag, and merge the completed publication checkpoint to local
`main`. Do not implement queue v1, write the book, run a benchmark, push, or pull.

Acceptance is the `task.json` contract. End with `NEXT MODEL: LOW`.
