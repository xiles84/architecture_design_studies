# Execution brief — history import and status reconciliation

Use queue v1 to import completed EH-01, EH-02, Study 03, Study 04, and Study 05 work by
reference to their existing handoffs, receipts, commits, tags, runs, and analyses. Never
rewrite an attributed artifact. Create a separate `ready` HIGH task for the Study 02/03
v2 validation that `CONTEXT.md` currently says remains open.

Archive the complete pre-edit `CONTEXT.md` under `docs/history/context/` using a clock-
generated UTC name. Replace the live file with one concise current state per study/task,
links to the queue/history, the benchmark lock status rule, current tags/runs, and open
gaps. Preserve historical facts through archive links instead of duplicate live receipts.

Update the root README and stale Study 02/03/05 status/navigation sections. Do not change
measurements or signed conclusions. Reorder methodology 11a/11b/12 headings while keeping
their text and meaning unchanged.

Validate links and reconcile every public status against tags, reports, analyses, and the
current main history. Submit for HIGH review.

NEXT MODEL: LOW
