# HIGH release review brief — book v1

Review the synthesis Decision Log first, then the evidence registry, book source, PDF,
build manifest, navigation, and current public statuses. Check every number and scope,
the direct-evidence/analogy boundary, cross-study comparisons, topology qualifications,
failed-cell handling, and superseded material. Review both the quick-choice beginner path
and the mechanism/reproduction expert path.

Material defects receive targeted correction tasks; equivalent stylistic preferences do
not. Rebuild and visually inspect the final PDF after corrections. Approve only when all
acceptance checks pass, then integrate, tag `repo/data-architecture-book-v1`, and verify
reachability from local `main`. Never push or pull.

NEXT MODEL: HIGH
